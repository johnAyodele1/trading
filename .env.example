# Database Configuration
# Replace 'user', 'pass', 'localhost', '5432', and 'db_name' with your actual PostgreSQL credentials.
DATABASE_URL=postgresql://user:pass@localhost:5432/db_name

# Engine Mode (BACKTEST or LIVE)
ENGINE_MODE=BACKTEST
